{
  "name": "root",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna build",
    "publish": "lerna version minor --yes && lerna publish from-git --yes"
  }
}
